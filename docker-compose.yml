version: "3.9"

services:

  backend:
    build: ./server
    container_name: deepguard-backend
    ports:
      - "5000:5000"
    restart: unless-stopped

  frontend:
    build: ./front
    container_name: deepguard-frontend
    ports:
      - "3000:3000"
    restart: unless-stopped 
